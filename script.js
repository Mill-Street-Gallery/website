const header=document.querySelector('.site-header');const sentinel=document.querySelector('.header-sentinel');if(header&&sentinel){new IntersectionObserver(([entry])=>header.classList.toggle('is-stuck',!entry.isIntersecting),{threshold:0}).observe(sentinel)}const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));toggle.textContent=open?'Close':'Menu'})}const dialog=document.querySelector('.mailing-dialog');const opener=document.querySelector('[data-open-mailing]');if(dialog&&opener){opener.addEventListener('click',()=>dialog.showModal());dialog.querySelector('.dialog-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()})}const form=document.querySelector('#mailing-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);location.href=`mailto:info@millstreetgallery.org?subject=${encodeURIComponent('Mill Street Gallery mailing list')}&body=${encodeURIComponent(`Name: ${d.get('name')}\nEmail: ${d.get('email')}\nProfession: ${d.get('profession')||''}\nCity: ${d.get('city')||''}`)}`})}
